import type {MetadataRoute} from "next";
import {createClient} from "@supabase/supabase-js";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://agriva.com.bd";const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;let articles:any[]=[];if(u&&k){const s=createClient(u,k);const {data}=await s.from("content_drafts").select("slug,updated_at").eq("status","published").eq("content_type","article");articles=data||[]}return [{url:base,lastModified:new Date(),changeFrequency:"daily",priority:1},{url:`${base}/blog`,changeFrequency:"daily",priority:.8},...articles.map(a=>({url:`${base}/blog/${a.slug}`,lastModified:a.updated_at?new Date(a.updated_at):new Date(),changeFrequency:"monthly" as const,priority:.7}))]}
