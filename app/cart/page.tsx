"use client";
import {useMemo,useState} from "react";
import Link from "next/link";
type Item={id:string;name:string;price:number;qty:number};
export default function Cart(){const [items,setItems]=useState<Item[]>([]);const total=useMemo(()=>items.reduce((s,i)=>s+i.price*i.qty,0),[items]);function remove(id:string){setItems(x=>x.filter(i=>i.id!==id))}return <main className="min-h-screen bg-[#f5f7f2] px-6 py-12 text-[#17351f]"><div className="mx-auto max-w-5xl"><p className="text-sm font-bold uppercase tracking-[.18em] text-[#b58b28]">AGRIVA Marketplace</p><h1 className="mt-3 text-4xl font-black">Your Cart</h1>{items.length===0?<section className="mt-8 rounded-3xl bg-white p-10 text-center shadow-sm"><h2 className="text-2xl font-black">Your cart is empty</h2><p className="mt-2 text-[#647468]">Add farmer products from the marketplace to start checkout.</p><Link href="/marketplace" className="mt-6 inline-block rounded-xl bg-[#17351f] px-6 py-3 font-bold text-white">Browse marketplace</Link></section>:<section className="mt-8 grid gap-6 md:grid-cols-[1fr_320px]"><div className="rounded-3xl bg-white p-6 shadow-sm">{items.map(i=><div key={i.id} className="flex items-center justify-between gap-4 border-b py-4 last:border-0"><div><b>{i.name}</b><p className="text-sm text-[#647468]">Qty {i.qty}</p></div><div className="text-right"><b>৳{(i.price*i.qty).toFixed(2)}</b><button onClick={()=>remove(i.id)} className="ml-4 text-sm font-bold underline">Remove</button></div></div>)}</div><aside className="h-fit rounded-3xl bg-white p-6 shadow-sm"><p className="text-sm text-[#647468]">Subtotal</p><p className="mt-2 text-3xl font-black">৳{total.toFixed(2)}</p><Link href="/checkout" className="mt-6 block rounded-xl bg-[#17351f] px-5 py-3 text-center font-bold text-white">Proceed to checkout</Link></aside></section>}</div></main>}
