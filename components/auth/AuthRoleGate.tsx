"use client";
import {useState} from "react";
const roles=[{id:"farmer",title:"Farmer",desc:"Sell farm products, manage crops and set reseller commission."},{id:"reseller",title:"Reseller",desc:"Promote farmer products and track eligible referral commission."},{id:"admin",title:"Admin",desc:"Manage marketplace, content, users and AI intelligence."}];
export default function AuthRoleGate(){const [role,setRole]=useState("farmer");const [email,setEmail]=useState("");const [notice,setNotice]=useState("");function submit(e:React.FormEvent){e.preventDefault();if(!email)return;setNotice(`${role} account flow selected for ${email}. Connect Supabase Auth to enable real sign-in.`)}return <div className="mt-8 grid gap-6 lg:grid-cols-[.9fr_1.1fr]"><div className="space-y-3">{roles.map(r=><button key={r.id} onClick={()=>setRole(r.id)} className={`w-full rounded-2xl p-5 text-left ${role===r.id?"bg-[#17351f] text-white":"bg-white shadow-sm"}`}><b className="text-lg">{r.title}</b><p className={`mt-1 text-sm ${role===r.id?"text-white/65":"text-[#647468]"}`}>{r.desc}</p></button>)}</div><form onSubmit={submit} className="rounded-3xl bg-white p-6 shadow-sm"><p className="text-sm text-[#647468]">Selected role</p><h2 className="mt-1 text-2xl font-black capitalize">{role}</h2><label className="mt-6 block text-sm font-bold">Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"/></label><label className="mt-4 block text-sm font-bold">Password<input type="password" required minLength={6} placeholder="••••••••" className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"/></label><button className="mt-5 w-full rounded-xl bg-[#17351f] px-5 py-3 font-bold text-white">Continue as {role}</button>{notice&&<div className="mt-4 rounded-xl bg-[#eef5ed] p-4 text-sm font-semibold">{notice}</div>}<p className="mt-5 text-xs leading-5 text-[#647468]">Security architecture: Supabase Auth session + profiles table + role-based access policies. Never trust a client-side role value for authorization.</p></form></div>}
