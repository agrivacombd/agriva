"use client";
import { useMemo, useState } from "react";

type Observation={date:string;crop:string;market:string;price:number};
const crops=["Rice","Wheat","Maize","Potato","Onion","Tomato","Jute"];
const markets=["Dhaka","Rajshahi","Rangpur","Bogura","Chattogram","Khulna"];
export default function CropMarketPrice(){const [crop,setCrop]=useState("Rice");const [market,setMarket]=useState("Rajshahi");const [price,setPrice]=useState("");const [date,setDate]=useState(new Date().toISOString().slice(0,10));const [items,setItems]=useState<Observation[]>([]);const recent=useMemo(()=>items.filter(x=>x.crop===crop&&x.market===market),[items,crop,market]);const avg=recent.length?recent.reduce((s,x)=>s+x.price,0)/recent.length:0;function add(){const p=Number(price);if(!p)return;setItems([{date,crop,market,price:p},...items]);setPrice("");}return <section className="mt-8 grid gap-6 lg:grid-cols-[1fr_1.2fr]"><div className="rounded-3xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Price observation</h2><div className="mt-5 space-y-4"><label className="block text-sm font-bold">Crop<select value={crop} onChange={e=>setCrop(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal">{crops.map(x=><option key={x}>{x}</option>)}</select></label><label className="block text-sm font-bold">Market<select value={market} onChange={e=>setMarket(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal">{markets.map(x=><option key={x}>{x}</option>)}</select></label><label className="block text-sm font-bold">Date<input type="date" value={date} onChange={e=>setDate(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"/></label><label className="block text-sm font-bold">Observed price (৳ / kg)<input type="number" min="0" step="0.01" value={price} onChange={e=>setPrice(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"/></label><button onClick={add} className="w-full rounded-xl bg-[#17351f] px-5 py-3 font-bold text-white">Add observation</button></div></div><div className="rounded-3xl bg-[#17351f] p-6 text-white"><p className="text-sm text-white/60">Selected market</p><h2 className="mt-1 text-2xl font-black">{crop} · {market}</h2><div className="mt-6 rounded-2xl bg-white/10 p-5"><p className="text-sm text-white/60">Recorded average</p><p className="mt-2 text-4xl font-black">{avg?`৳${avg.toFixed(2)}/kg`:"No data yet"}</p><p className="mt-2 text-xs text-white/50">{recent.length} local observation{recent.length===1?"":"s"} in this browser</p></div><h3 className="mt-7 font-black">Recent observations</h3><div className="mt-3 space-y-2">{recent.length?recent.slice(0,8).map((x,i)=><div key={`${x.date}-${i}`} className="flex justify-between rounded-xl bg-white/10 px-4 py-3 text-sm"><span>{x.date}</span><b>৳{x.price.toFixed(2)}/kg</b></div>):<p className="rounded-xl bg-white/10 p-4 text-sm text-white/60">Add a verified observation to start a local history.</p>}</div><p className="mt-6 text-xs leading-5 text-white/45">Prototype: observations are browser-only. Production should use verified data sources and Supabase persistence.</p></div></section>}
