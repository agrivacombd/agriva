"use client";
import { useMemo, useState } from "react";
const fields=["Seed","Fertilizer","Pesticide","Labour","Irrigation","Land preparation","Transport","Other"];
export default function FarmCostCalculator(){const [area,setArea]=useState("1");const [production,setProduction]=useState("1000");const [costs,setCosts]=useState<Record<string,string>>(Object.fromEntries(fields.map(x=>[x,"0"])));const total=useMemo(()=>fields.reduce((s,k)=>s+(Number(costs[k])||0),0),[costs]);const perAcre=Number(area)>0?total/Number(area):0;const perKg=Number(production)>0?total/Number(production):0;const set=(k:string,v:string)=>setCosts({...costs,[k]:v});return <section className="mt-8 grid gap-6 lg:grid-cols-2"><div className="rounded-3xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Farm inputs</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold">Land area (acre)<input type="number" min="0" step="0.01" value={area} onChange={e=>setArea(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"/></label><label className="text-sm font-bold">Expected production (kg)<input type="number" min="0" step="0.1" value={production} onChange={e=>setProduction(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"/></label>{fields.map(k=><label key={k} className="text-sm font-bold">{k} cost (৳)<input type="number" min="0" step="1" value={costs[k]} onChange={e=>set(k,e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"/></label>)}</div></div><div className="rounded-3xl bg-[#17351f] p-6 text-white"><p className="text-sm text-white/70">Estimated farm cost</p><p className="mt-3 text-5xl font-black">৳{total.toLocaleString(undefined,{maximumFractionDigits:0})}</p><div className="mt-7 space-y-3">{[["Cost per acre",perAcre],["Cost per kg",perKg]].map(([l,x])=><div key={l} className="flex justify-between border-b border-white/15 py-3"><span className="text-white/70">{l}</span><b>৳{Number(x).toFixed(2)}</b></div>)}</div><div className="mt-8 rounded-2xl bg-white/10 p-4 text-sm leading-6 text-white/70">For better planning, use actual expenses where possible. These calculations are estimates and do not guarantee profit.</div></div></section>}
